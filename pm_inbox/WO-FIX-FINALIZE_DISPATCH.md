# WO-FIX-FINALIZE: Complete Fix Phase & Stabilize Test Suite

**Authority:** PM (Opus 4.6)
**Date:** 2026-02-14
**Status:** READY for dispatch
**Scope:** Test, commit, and finalize all fix WO work. Implement 2 remaining WOs. Stabilize test suite.
**Estimated file touches:** ~6 files

---

## Context

The bone-layer fix phase has produced 13 fix WOs. A builder has implemented most of them across multiple commits and uncommitted changes. This WO covers the remaining cleanup to reach GREEN.

**Reference:** `pm_inbox/FIX_WO_DISPATCH_PACKET.md` — full WO specifications for all 13 fix WOs.

---

## Task 1: Verify and commit uncommitted fix WO changes

The following files have uncommitted changes implementing fix WOs:

| File | Fix WO | Bug IDs |
|------|--------|---------|
| `aidm/schemas/conditions.py` | WO-FIX-03 + WO-FIX-04 | BUG-3/4/5/6/7 |
| `aidm/core/spell_resolver.py` | WO-FIX-06 | BUG-C-002 |
| `aidm/core/play_loop.py` | WO-FIX-06 | BUG-C-002 |
| `aidm/core/feat_resolver.py` | WO-FIX-14 | I-AMB-02 |
| `aidm/core/attack_resolver.py` | WO-FIX-03 consumer | BUG-3/4 |
| `aidm/core/full_attack_resolver.py` | WO-FIX-03 consumer | BUG-3/4 |
| `aidm/core/duration_tracker.py` | Related fix | — |

**Steps:**
1. Run `python -m pytest tests/ -x -q` to verify ALL tests pass with uncommitted changes.
2. If tests fail, diagnose and fix. Refer to `FIX_WO_DISPATCH_PACKET.md` for intended behavior.
3. If tests pass, commit with message: `fix: BUG-3/4/5/6/7, C-002, I-AMB-02 — conditions AC, concentration DC, TWF penalties`

---

## Task 2: Implement WO-FIX-11 (action cost table)

**Bug:** G-PLAY-71-86
**File:** `aidm/core/play_loop.py` (action cost table, around line 71-86)
**Spec:** See `pm_inbox/FIX_WO_DISPATCH_PACKET.md` section "WO-FIX-11"

Trip, Disarm, and Grapple are currently classified as `"standard"` action type. SRD says they are `"varies"` (can substitute for any melee attack).

**Fix:** Change the action type entries:
```python
"trip": "varies",      # was "standard"
"disarm": "varies",    # was "standard"
"grapple": "varies",   # was "standard"
```

If the action parser doesn't support `"varies"`, add it as a recognized type. If full "varies" support (attack substitution in full attack) is too large, change the classification and add a TODO comment. The classification fix is the correctness fix.

**Tests:** Verify action type lookups return "varies" for trip/disarm/grapple.

**Commit:** `fix: G-PLAY-71-86 — trip/disarm/grapple action type = varies`

---

## Task 3: Implement WO-FIX-12 partial (XP table levels 11-20)

**Bugs:** BUG-F2, BUG-F3
**File:** `aidm/schemas/leveling.py` (lines 291-308)
**Spec:** See `pm_inbox/FIX_WO_DISPATCH_PACKET.md` section "WO-FIX-12"

Levels 11-20 XP entries are generated by a formula that doesn't match DMG Table 2-6. Replace with hardcoded values from the DMG table.

**Steps:**
1. Look up DMG Table 2-6 (page 38) values for levels 11-20. Cross-reference with `Vault/` SRD files if available.
2. Replace the formula-generated loop with correct hardcoded values.
3. Add tests spot-checking 5+ entries across levels 11-20.

**Commit:** `fix: BUG-F2/F3 — hardcoded XP table levels 11-20 from DMG`

---

## Task 4: Test suite stabilization

Two known test interaction issues:

### 4a: Spark stress test contamination
- 6 tests in `tests/test_spark_integration_stress.py` fail when run as part of the full suite but pass individually.
- Root cause: likely test-order-dependent state pollution (global state, module-level singletons, or fixture side effects).
- **Fix:** Isolate the contamination source. Add proper teardown/cleanup. Tests must pass in full suite (`python -m pytest tests/ -x -q`).

### 4b: TTS/WAV cross-contamination (conditional)
- Operator reported TTS tests fail with fix WO code changes applied. If this persists after Tasks 1-3 are committed, investigate.
- May be related to changes in attack_resolver or other modules affecting test fixture state.

**Commit:** `fix: test isolation — spark stress + TTS contamination`

---

## Task 5: Final verification

After all fixes committed:
1. Run `python -m pytest tests/ -q` — all tests must pass.
2. Run `python -m pytest tests/ -x -q --tb=short` — no failures.
3. Report final test count and any skips.
4. Report `git log --oneline` for all new commits made.

---

## Builder Protocol

- Read `pm_inbox/FIX_WO_DISPATCH_PACKET.md` for full bug specifications (Tasks 2 and 3).
- Do NOT refactor surrounding code. Surgical fixes only.
- Do NOT touch files not listed above.
- If a test was testing WRONG behavior, update the test. If your fix introduces a regression, fix the fix.
- Check research docs before fixing (per dispatch packet universal instructions).
- Commit each logical unit separately (Tasks 1, 2, 3, 4 as separate commits).

---

## Completion Report

When done, provide:
```
COMMITS: [list of commits with hashes]
TEST COUNT: [total passed / failed / skipped]
STOPLIGHT: [GREEN if all pass, YELLOW if skips only, RED if failures]
ISSUES: [any problems encountered or unresolved items]
```
